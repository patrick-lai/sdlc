All public document access must be tenant isolated. Prices cross the API as integer cents and the client divides by 100 for display. Failed or cancelled jobs must release capacity. Search results must belong to the most recently selected query. This task deliberately removes the legacy label branch, keeping the selected modern branch. Keep public function signatures. Tests use Python standard library only.
