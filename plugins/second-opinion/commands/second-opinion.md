---
description: Run a cheap native second-opinion review of the current change
argument-hint: "[on|off|once]"
---

Follow the installed `second-opinion` skill.

If `$ARGUMENTS` is `on` or `off`, set the session mode accordingly and do not review unless `on` also implies always-on for later turns.
If `$ARGUMENTS` is empty or `once`, run one explicit review now.
